{
  "name": "step-04",
  "version": "0.1.0",
  "description": "Step 04 — v3 scales: composition, gateway, contracts, idempotency. The v2 catalog from step-03/solutions/3.8/ mounted unchanged + new arena-server + a typed MCP gateway built from scratch. Five iterations 4.0 → 4.4.",
  "private": true,
  "type": "module",
  "scripts": {
    "dev:arena": "bun --hot run arena-server/src/server.ts",
    "dev:gateway": "bun --hot run gateway/src/server.ts",
    "smoke": "bun run scripts/smoke.ts",
    "typecheck": "bunx tsc --noEmit"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "postgres": "^3.4.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.6.0"
  },
  "engines": {
    "bun": ">=1.1.0"
  },
  "author": "Horacio Gonzalez <@LostInBrittany>",
  "license": "MIT"
}
