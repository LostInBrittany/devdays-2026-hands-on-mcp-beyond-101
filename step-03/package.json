{
  "name": "step-03",
  "version": "0.1.0",
  "description": "Step 03 — v2 shaped: the RAGmonsters rebuild. Three primitives (typed Tools, cacheable Resources, codified Prompts), designed deliberately. Connects to pglite/'s PGlite-over-socket (the same instance step-02 used) so it's the same database, different server — that's the lesson.",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun --hot run src/server.ts",
    "start": "bun run src/server.ts",
    "typecheck": "bunx tsc --noEmit"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "postgres": "^3.4.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.6.0"
  },
  "engines": {
    "bun": ">=1.1.0"
  },
  "author": "Horacio Gonzalez <@LostInBrittany>",
  "license": "MIT"
}
